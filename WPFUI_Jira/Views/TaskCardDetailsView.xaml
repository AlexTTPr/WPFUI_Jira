<ui:ContentDialog
    x:Class="WPFUI_Jira.Views.TaskCardDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPFUI_Jira.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="TaskCardDetialsView"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <Label Content="Название задачи" />

        <TextBox Text="{Binding ViewModel.Title}" />
        <Label Content="Описание задачи" />
        <TextBox Text="{Binding ViewModel.Description}" />
        <Label Content="Исполнитель" />
        <TextBox Text="{Binding ViewModel.Executor}" />
        <Label Content="Срок выполнения" />
        <Label Content="{Binding ViewModel.CreationTime, StringFormat='Задача была создана: {0}'}" />
        <Label Content="{Binding ViewModel.ExpirationTime, StringFormat='Срок выполнения истекает в: {0}'}" />
        <Button
            HorizontalAlignment="Left"
            Command="{Binding ViewModel.CloseCommand}"
            Content="Закрыть" />
    </StackPanel>
</ui:ContentDialog>
